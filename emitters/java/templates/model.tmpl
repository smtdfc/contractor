  public static class {{.Name}}{{.TypeParams}} {
{{- range $index, $field := .Fields }}
    {{ $field }}
{{- end }}

    public static {{.Name}} fromJson(String json, Function<String, {{.Name}}> decoder) {
      return decoder.apply(json);
    }

    public static {{.Name}} fromJson(String json) {
      throw new UnsupportedOperationException("No default JSON decoder for Java target. Use fromJson(json, decoder).");
    }

{{- if .ConstructorCode }}
{{.ConstructorCode}}
{{- end }}
{{.ValidatorCode}}
  }
