type {{.Name}}{{.TypeParams}} struct {
{{- range $index, $field := .Fields }}
    {{ $field }}
{{- end }}
}

func {{.Name}}FromJSON{{.TypeParams}}(jsonText string) (*{{.Name}}{{.Generics}}, error) {
    var value {{.Name}}{{.Generics}}

    if err := json.Unmarshal([]byte(jsonText), &value); err != nil {
        return nil, err
    }

    return &value, nil
}

